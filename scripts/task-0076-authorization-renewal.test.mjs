import assert from 'node:assert/strict';import fs from 'node:fs';import test from 'node:test';import {evaluateRenewal} from './task-0076-authorization-renewal.mjs';const now=new Date('2026-08-01T00:00:00Z');const good=()=>JSON.parse(fs.readFileSync('contracts/task-0076-authorization-renewal.json'));test('bound renewal passes',()=>assert.equal(evaluateRenewal({renewal:good(),now}).status,'passed'));for(const m of [x=>x.scope=' ',x=>x.evidence_ref='evidence://',x=>x.as_of='2027-01-01T00:00:00Z',x=>x.expires_at='2026-08-01T00:00:00Z',x=>x.approvals[0].actor=' ',x=>x.custody_contract='bad'])test('renewal fails closed',()=>{const x=good();m(x);assert.equal(evaluateRenewal({renewal:x,now}).status,'failed')});

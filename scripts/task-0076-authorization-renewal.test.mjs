import assert from 'node:assert/strict';import fs from 'node:fs';import test from 'node:test';import {evaluateRenewal} from './task-0076-authorization-renewal.mjs';const good=()=>JSON.parse(fs.readFileSync('contracts/task-0076-authorization-renewal.json'));test('renewal passes',()=>assert.equal(evaluateRenewal({renewal:good()}).status,'passed'));for(const m of [x=>x.approvals=[],x=>x.expires_at='2020-01-01T00:00:00Z',x=>x.external_prerequisites=[],x=>x.deployment='performed'])test('renewal fails closed',()=>{const x=good();m(x);assert.equal(evaluateRenewal({renewal:x}).status,'failed')});

import test from 'node:test'; import assert from 'node:assert/strict'; import {evaluateDREvidence} from './task-0036-dr-evidence.mjs';
const good=()=>({backup:{managed:true,encrypted:true,integrity_verified:true,evidence_ref:'evidence://backup/1',observed_lag_minutes:4,tenant_isolated:true},restore:{native_tooling:true,schema_verified:true,data_probe:true,evidence_ref:'evidence://backup/1',observed_minutes:12,tenant_isolated:true},targets:{rpo_minutes:5,rto_minutes:15},failover:{managed:true,tested:true,corrective_forward:true,target:'cell://secondary'}});
test('complete DR evidence passes',()=>assert.equal(evaluateDREvidence(good()).status,'passed'));
for(const m of [x=>delete x.backup,x=>{x.restore.evidence_ref='tampered'},x=>{x.backup.observed_lag_minutes=9},x=>{x.restore.native_tooling=false},x=>{x.failover.corrective_forward=false},x=>{x.backup.tenant_isolated=false}])test('DR evidence fails closed',()=>{const x=good();m(x);assert.equal(evaluateDREvidence(x).status,'failed');});

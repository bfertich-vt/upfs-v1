import crypto from'node:crypto';import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),h=x=>crypto.createHash('sha256').update(JSON.stringify(x)).digest('hex'),d=x=>({scope:x?.scope,source_refs:x?.source_refs,evidence:x?.evidence,deployment:x?.deployment});export function evaluateTenantChaos({contract,synthetic_only=true}={}){const c=contract,e=c?.evidence,q=[],a=(n,o)=>q.push({name:n,status:o?'passed':'failed'}),s=['specs/04_schema/identity_tenant_model.md','specs/12_testing/test_strategy.md'];a('boundary',synthetic_only&&c?.deployment==='not-performed');a('source',Array.isArray(c?.source_refs)&&JSON.stringify(c.source_refs)===JSON.stringify(s)&&s.every(x=>fs.existsSync(path.join(root,x))));a('chaos',c?.schema_version==='upfs.tenant-chaos.v1'&&c?.scope?.tenant_id?.trim()===c.scope.tenant_id&&c?.scope?.environment_id?.trim()===c.scope.environment_id&&e?.ref?.startsWith('evidence://')&&['reads','writes','projections','exports','support','recovery'].every(x=>e?.[x]===true)&&e?.state==='blocked'&&e?.replay==='replayed'&&e?.conflict==='rejected');a('integrity',c?.digest===h(d(c)));return{status:q.every(x=>x.status==='passed')?'passed':'failed',decision:'NO-GO_EXTERNAL_PREREQUISITES',checks:q}}

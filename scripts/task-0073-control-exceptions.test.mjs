import assert from 'node:assert/strict';import fs from 'node:fs';import test from 'node:test';import {evaluateControlExceptions} from './task-0073-control-exceptions.mjs';const good=()=>JSON.parse(fs.readFileSync('contracts/task-0073-control-exceptions.json'));test('explicit lifecycle passes',()=>assert.equal(evaluateControlExceptions({register:good()}).status,'passed'));for(const m of [x=>x.exceptions[0].lifecycle.splice(0,1),x=>x.exceptions[0].expires_at=x.as_of,x=>x.exceptions[0].remediation.evidence_ref='bad',x=>x.exceptions[0].gate_bypass=true])test('expiry remediation and lifecycle fail closed',()=>{const x=good();m(x);assert.equal(evaluateControlExceptions({register:x}).status,'failed')});

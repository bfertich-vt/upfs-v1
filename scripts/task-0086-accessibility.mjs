import crypto from'node:crypto';import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),h=x=>crypto.createHash('sha256').update(JSON.stringify(x)).digest('hex'),data=x=>({scope:x.scope,source_refs:x.source_refs,evidence:x.evidence,state:x.state,deployment:x.deployment});export function evaluateAccessibility({contract,synthetic_only=true}={}){const c=contract,q=[],a=(n,o)=>q.push({name:n,status:o?'passed':'failed'}),s=['specs/02_ui/console_experience.md','docs/MASTER_PLAN.md'],e=c?.evidence;a('boundary',synthetic_only&&c?.state==='blocked'&&c?.deployment==='not-performed');a('source',JSON.stringify(c?.source_refs)===JSON.stringify(s)&&s.every(x=>fs.existsSync(path.join(root,x))));a('evidence',c?.schema_version==='upfs.accessibility.v1'&&c?.scope?.tenant&&c?.scope?.environment&&e?.ref?.startsWith('evidence://')&&['keyboard','focus','labels','contrast','semantics','responsive','unavailable_states'].every(x=>e?.[x]===true));a('integrity',c?.digest===h(data(c)));return{status:q.every(x=>x.status==='passed')?'passed':'failed',decision:'NO-GO_EXTERNAL_PREREQUISITES',checks:q}}

import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');export function evaluateAccessReview({review,synthetic_only=true,deployment='not-performed'}={}){const c=[];const a=(n,o)=>c.push({name:n,status:o?'passed':'failed'});const b=v=>typeof v==='string'&&v.trim().length>0;a('boundary',synthetic_only&&deployment==='not-performed');a('periodic',b(review?.period)&&b(review?.review_ref));a('least-privilege',review?.least_privilege===true);a('joiner-mover-leaver',review?.joiner===true&&review?.mover===true&&review?.leaver===true);a('offboarding',review?.offboarding_verified===true);a('exceptions',Array.isArray(review?.exceptions));a('remediation',review?.remediation_tracked===true&&b(review?.remediation_ref));a('audit',b(review?.audit_ref));return{schema_version:'upfs.access-review-report.v1',status:c.every(x=>x.status==='passed')?'passed':'failed',decision:'NO-GO_EXTERNAL_PREREQUISITES',synthetic_only,deployment,checks:c};}if(process.argv[1]&&path.resolve(process.argv[1])===path.resolve(fileURLToPath(import.meta.url))){const r=evaluateAccessReview();fs.writeFileSync(path.join(root,'artifacts/task-0054-access-review-report.json'),JSON.stringify(r,null,2)+'\n');console.log(`TASK-0054 access review: ${r.decision}`);}

import crypto from'node:crypto';import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),h=x=>crypto.createHash('sha256').update(JSON.stringify(x)).digest('hex'),d=x=>({scope:x?.scope,source_refs:x?.source_refs,action:x?.action,deployment:x?.deployment});export function evaluateAdminAction({contract,synthetic_only=true}={}){const c=contract,a=c?.action,q=[],p=(n,o)=>q.push({name:n,status:o?'passed':'failed'});p('boundary',synthetic_only&&c?.deployment==='not-performed');p('source',Array.isArray(c?.source_refs)&&JSON.stringify(c.source_refs)==='["specs/14_admin_control_plane/admin_control_plane.md"]'&&fs.existsSync(path.join(root,c.source_refs[0])));p('controls',c?.schema_version==='upfs.admin-action.v1'&&c?.scope?.tenant_id?.trim()===c.scope.tenant_id&&c?.scope?.environment_id?.trim()===c.scope.environment_id&&a?.ref?.startsWith('evidence://')&&['approval','dual_control','idempotency','audit','rollback','unavailable','direct_infra_denied'].every(x=>a?.[x]===true)&&a?.state==='blocked'&&a?.replay==='replayed'&&a?.conflict==='rejected');p('integrity',c?.digest===h(d(c)));return{status:q.every(x=>x.status==='passed')?'passed':'failed',decision:'NO-GO_EXTERNAL_PREREQUISITES',checks:q}}

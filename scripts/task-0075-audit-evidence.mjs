import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');export function evaluateAuditEvidence({review,synthetic_only=true}={}){const c=[];const a=(n,o)=>c.push({name:n,status:o?'passed':'failed'});const r=review?.records;a('boundary',synthetic_only&&review?.deployment==='not-performed');a('schema',review?.schema_version==='upfs.audit-evidence-review.v1');a('immutable-tenant-audit',Array.isArray(r)&&r.length&&r.every(x=>x.id&&x.tenant_scope&&x.action&&x.actor&&x.immutable===true&&String(x.evidence_ref).startsWith('evidence://')&&/^[a-f0-9]{64}$/.test(x.hash||'')));a('completeness',Array.isArray(review?.expected_sensitive_actions)&&review.expected_sensitive_actions.every(x=>r.some(y=>y.action===x)));return{status:c.every(x=>x.status==='passed')?'passed':'failed',decision:'NO-GO_EXTERNAL_PREREQUISITES',checks:c}}if(process.argv[1]&&path.resolve(process.argv[1])===path.resolve(fileURLToPath(import.meta.url))){const review=JSON.parse(fs.readFileSync(path.join(root,'contracts/task-0075-audit-evidence.json')));fs.writeFileSync(path.join(root,'artifacts/task-0075-audit-evidence.json'),JSON.stringify(evaluateAuditEvidence({review}),null,2)+'\n')}

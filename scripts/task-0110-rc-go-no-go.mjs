import crypto from'node:crypto';import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),sources=['docs/compliance/pilot-go-no-go.md','specs/11_soc2/soc2_readiness.md','contracts/task-0109-rc-evidence-index.json'],hash=x=>crypto.createHash('sha256').update(JSON.stringify(x)).digest('hex'),canon=x=>({scope:x?.scope,source_refs:x?.source_refs,evidence:x?.evidence,deployment:x?.deployment});export function evaluateRcGoNoGo({contract,synthetic_only=true}={}){const e=contract?.evidence,c=[],a=(n,p)=>c.push({name:n,status:p?'passed':'failed'});a('boundary',synthetic_only&&contract?.deployment==='not-performed');a('source',Array.isArray(contract?.source_refs)&&JSON.stringify(contract.source_refs)===JSON.stringify(sources)&&sources.every(x=>fs.existsSync(path.join(root,x))));a('decision',contract?.schema_version==='upfs.rc-go-no-go.v1'&&contract?.scope?.tenant_id==='synthetic-tenant'&&contract?.scope?.environment_id==='pilot'&&typeof e?.ref==='string'&&e.ref.startsWith('evidence://')&&e?.release_candidate_index==='contracts/task-0109-rc-evidence-index.json'&&e?.approval==='external-approval-required'&&e?.decision==='pause'&&e?.continuation==='not-authorized'&&e?.pause==='required'&&e?.stop==='not-required'&&e?.open_risks==='open'&&e?.external_prerequisites==='open'&&e?.claims==='no-production-or-certification'&&e?.state==='blocked'&&e?.replay==='replayed'&&e?.conflict==='rejected');a('integrity',contract?.digest===hash(canon(contract)));return{status:c.every(x=>x.status==='passed')?'passed':'failed',decision:'PAUSE_EXTERNAL_APPROVAL_AND_PREREQUISITES',checks:c}}

import assert from'node:assert/strict';import crypto from'node:crypto';import fs from'node:fs';import test from'node:test';import{evaluateTenantChaos}from'./task-0094-tenant-chaos.mjs';const g=()=>JSON.parse(fs.readFileSync('contracts/task-0094-tenant-chaos.json')),r=x=>x.digest=crypto.createHash('sha256').update(JSON.stringify({scope:x.scope,source_refs:x.source_refs,evidence:x.evidence,deployment:x.deployment})).digest('hex');test('tenant chaos passes',()=>assert.equal(evaluateTenantChaos({contract:g()}).status,'passed'));for(const m of[x=>{x.evidence.reads=false;r(x)},x=>{x.evidence.exports=false;r(x)},x=>{x.evidence.recovery=false;r(x)},x=>{x.evidence.replay='bad';r(x)},x=>{x.evidence.conflict='bad';r(x)},x=>{x.scope.tenant_id=' ';r(x)},x=>{x.deployment='performed';r(x)},x=>x.digest='bad'])test('tenant chaos fails closed',()=>{const x=g();m(x);assert.equal(evaluateTenantChaos({contract:x}).status,'failed')});

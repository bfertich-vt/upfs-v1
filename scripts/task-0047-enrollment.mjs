import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');export function evaluateEnrollment({request,synthetic_only=true,deployment='not-performed'}={}){const c=[];const a=(n,o)=>c.push({name:n,status:o?'passed':'failed'});const b=v=>typeof v==='string'&&v.trim().length>0;a('boundary',synthetic_only&&deployment==='not-performed');a('tenant-scope',b(request?.tenant_id)&&request?.environment?.trim()==='pilot');a('approval',request?.approval?.approved===true&&b(request.approval.approver));a('reversible',request?.reversible===true&&b(request.rollback_ref));a('audit',b(request?.audit_ref)&&b(request.actor));a('no-real-data',request?.synthetic_data===true);return{schema_version:'upfs.enrollment-report.v1',status:c.every(x=>x.status==='passed')?'passed':'failed',decision:'NO-GO_EXTERNAL_PREREQUISITES',synthetic_only,deployment,checks:c};}if(process.argv[1]&&path.resolve(process.argv[1])===path.resolve(fileURLToPath(import.meta.url))){const r=evaluateEnrollment();fs.writeFileSync(path.join(root,'artifacts/task-0047-enrollment-report.json'),JSON.stringify(r,null,2)+'\n');console.log(`TASK-0047 enrollment: ${r.decision}`);}

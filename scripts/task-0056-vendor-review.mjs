import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');export function evaluateVendor({review,synthetic_only=true,deployment='not-performed'}={}){const c=[];const a=(n,o)=>c.push({name:n,status:o?'passed':'failed'});const b=v=>typeof v==='string'&&v.trim().length>0;const rec=x=>b(x?.ref)&&b(x?.status)&&b(x?.owner);a('boundary',synthetic_only&&deployment==='not-performed');a('schema',review?.schema_version==='upfs.vendor-review.v1');for(const k of ['provider','owner','frequency','procedure','evidence_source','evidence_ref','reviewer','last_execution','next_execution'])a(k,b(review?.[k]));a('systems',Array.isArray(review?.systems)&&review.systems.length>0&&review.systems.every(b));a('incidents',Array.isArray(review?.incidents)&&review.incidents.every(rec));a('exceptions',Array.isArray(review?.exceptions)&&review.exceptions.every(rec));a('remediation',Array.isArray(review?.remediation));a('retention',Number.isInteger(review?.retention_days)&&review.retention_days>0);return{schema_version:'upfs.vendor-review-report.v1',status:c.every(x=>x.status==='passed')?'passed':'failed',decision:'NO-GO_EXTERNAL_PREREQUISITES',synthetic_only,deployment,checks:c};}if(process.argv[1]&&path.resolve(process.argv[1])===path.resolve(fileURLToPath(import.meta.url))){const review=JSON.parse(fs.readFileSync(path.join(root,'contracts/task-0056-vendor-review.json')));const r=evaluateVendor({review});fs.writeFileSync(path.join(root,'artifacts/task-0056-vendor-review-report.json'),JSON.stringify(r,null,2)+'\n');console.log(`TASK-0056 vendor: ${r.decision}`);}

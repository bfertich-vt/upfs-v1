import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');export function evaluateVendor({review,synthetic_only=true,deployment='not-performed'}={}){const c=[];const a=(n,o)=>c.push({name:n,status:o?'passed':'failed'});const b=v=>typeof v==='string'&&v.trim().length>0;a('boundary',synthetic_only&&deployment==='not-performed');a('scope',b(review?.scope_ref)&&b(review?.provider));a('risk',b(review?.risk_rating)&&b(review?.risk_ref));a('cadence',b(review?.cadence)&&b(review?.next_review));a('contract',b(review?.contract_ref));a('incidents',Array.isArray(review?.incidents));a('exceptions',Array.isArray(review?.exceptions));a('audit',b(review?.audit_ref));return{schema_version:'upfs.vendor-review-report.v1',status:c.every(x=>x.status==='passed')?'passed':'failed',decision:'NO-GO_EXTERNAL_PREREQUISITES',synthetic_only,deployment,checks:c};}if(process.argv[1]&&path.resolve(process.argv[1])===path.resolve(fileURLToPath(import.meta.url))){const r=evaluateVendor();fs.writeFileSync(path.join(root,'artifacts/task-0056-vendor-review-report.json'),JSON.stringify(r,null,2)+'\n');console.log(`TASK-0056 vendor: ${r.decision}`);}

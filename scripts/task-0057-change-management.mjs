import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');export function evaluateChange({change,synthetic_only=true,deployment='not-performed'}={}){const c=[];const a=(n,o)=>c.push({name:n,status:o?'passed':'failed'});const b=v=>typeof v==='string'&&v.trim().length>0;a('boundary',synthetic_only&&deployment==='not-performed');a('review',b(change?.review_ref)&&b(change?.risk));a('approvals',Array.isArray(change?.approvals)&&change.approvals.length>0&&change.approvals.every(x=>b(x.actor)&&b(x.ref)));a('artifact',change?.immutable===true&&b(change?.artifact_digest));a('rollback',b(change?.rollback_ref)&&change?.corrective_forward===true);a('emergency',change?.emergency===true?b(change?.retrospective_ref):true);a('audit',b(change?.audit_ref));return{schema_version:'upfs.change-report.v1',status:c.every(x=>x.status==='passed')?'passed':'failed',decision:'NO-GO_EXTERNAL_PREREQUISITES',synthetic_only,deployment,checks:c};}if(process.argv[1]&&path.resolve(process.argv[1])===path.resolve(fileURLToPath(import.meta.url))){const r=evaluateChange();fs.writeFileSync(path.join(root,'artifacts/task-0057-change-report.json'),JSON.stringify(r,null,2)+'\n');console.log(`TASK-0057 change: ${r.decision}`);}

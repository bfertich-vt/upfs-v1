import assert from 'node:assert/strict';import fs from 'node:fs';import test from 'node:test';import {evaluatePauseResume} from './task-0077-pause-resume.mjs';const good=()=>JSON.parse(fs.readFileSync('contracts/task-0077-pause-resume.json'));test('pause resume passes',()=>assert.equal(evaluatePauseResume({workflow:good()}).status,'passed'));for(const m of [x=>x.idempotency_key='',x=>x.pause.approved=false,x=>x.resume.approval=false,x=>x.reversible=false])test('pause resume fails closed',()=>{const x=good();m(x);assert.equal(evaluatePauseResume({workflow:x}).status,'failed')});

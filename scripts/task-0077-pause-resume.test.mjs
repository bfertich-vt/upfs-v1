import assert from 'node:assert/strict';import fs from 'node:fs';import test from 'node:test';import {evaluatePauseResume} from './task-0077-pause-resume.mjs';const good=()=>JSON.parse(fs.readFileSync('contracts/task-0077-pause-resume.json'));test('transition ledger passes',()=>assert.equal(evaluatePauseResume({workflow:good()}).status,'passed'));for(const m of [x=>x.transitions[0].tenant='other',x=>x.replays[0].result='new',x=>x.conflicts[0].rejected=false,x=>x.transitions[1].approval.approved=false,x=>x.digest='bad'])test('transition ledger fails closed',()=>{const x=good();m(x);assert.equal(evaluatePauseResume({workflow:x}).status,'failed')});

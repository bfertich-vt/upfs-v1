import assert from'node:assert/strict';import crypto from'node:crypto';import fs from'node:fs';import test from'node:test';import{evaluateRestoreRetention}from'./task-0093-restore-retention.mjs';const g=()=>JSON.parse(fs.readFileSync('contracts/task-0093-restore-retention.json')),r=x=>x.digest=crypto.createHash('sha256').update(JSON.stringify({scope:x.scope,source_refs:x.source_refs,evidence:x.evidence,state:x.state,deployment:x.deployment})).digest('hex');test('restore retention passes',()=>assert.equal(evaluateRestoreRetention({contract:g()}).status,'passed'));for(const m of[x=>{x.evidence.legal_hold=false;r(x)},x=>{x.evidence.deletion_interaction=false;r(x)},x=>{x.evidence.immutable_reconciliation=false;r(x)},x=>{x.scope.tenant_id=' ';r(x)},x=>{x.deployment='performed';r(x)},x=>x.digest='bad'])test('restore retention fails closed',()=>{const x=g();m(x);assert.equal(evaluateRestoreRetention({contract:x}).status,'failed')});

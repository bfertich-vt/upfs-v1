import assert from'node:assert/strict';import crypto from'node:crypto';import fs from'node:fs';import test from'node:test';import{evaluateSloChange}from'./task-0090-slo-change.mjs';const g=()=>JSON.parse(fs.readFileSync('contracts/task-0090-slo-change.json')),r=x=>x.digest=crypto.createHash('sha256').update(JSON.stringify({scope:x.scope,source_refs:x.source_refs,review:x.review,state:x.state,deployment:x.deployment})).digest('hex');test('slo change passes',()=>assert.equal(evaluateSloChange({contract:g()}).status,'passed'));for(const m of[x=>{x.review.impact_analysis=false;r(x)},x=>{x.review.approval=false;r(x)},x=>{x.review.route_owner=' ';r(x)},x=>{x.review.rollback=false;r(x)},x=>{x.review.immutable_evidence=false;r(x)},x=>{x.deployment='performed';r(x)},x=>x.digest='bad'])test('slo change fails closed',()=>{const x=g();m(x);assert.equal(evaluateSloChange({contract:x}).status,'failed')});

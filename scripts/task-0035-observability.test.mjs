import test from 'node:test'; import assert from 'node:assert/strict'; import {evaluateObservability} from './task-0035-observability.mjs';
const good=()=>({provider:{managed:true,identity:'external-managed-observability'},telemetry:{tenant_scoped:true,redacted:true,cross_tenant_isolation:true,metrics:true,logs:true,traces:true},alerts:{slo_defined:true,error_rate:true,latency:true,paging_route:true},incident:{sequence:['detect','pause','abort','recover'],rollback_verified:true,corrective_forward:true},audit:{append_only:true,actor_bound:true,tenant_bound:true,retained:true}});
test('complete observability evidence passes',()=>assert.equal(evaluateObservability(good()).status,'passed'));
for(const m of [x=>delete x.provider,x=>{x.telemetry.tenant_scoped=false},x=>{x.alerts.slo_defined=false},x=>{x.incident.sequence=['detect','recover']},x=>{x.audit.append_only=false},x=>{x.synthetic_only=false}])test('observability evidence fails closed',()=>{const x=good();m(x);assert.equal(evaluateObservability(x).status,'failed');});

import assert from 'node:assert/strict';import fs from 'node:fs';import test from 'node:test';import {evaluateCustody} from './task-0074-evidence-custody.mjs';const good=()=>JSON.parse(fs.readFileSync('contracts/task-0074-evidence-custody.json'));test('custody index passes',()=>assert.equal(evaluateCustody({index:good()}).status,'passed'));for(const m of [x=>x.items[0].sha256='bad',x=>x.items[0].retention_days=0,x=>x.items[0].access='public',x=>x.items[0].lineage=[]])test('custody fails closed',()=>{const x=good();m(x);assert.equal(evaluateCustody({index:x}).status,'failed')});

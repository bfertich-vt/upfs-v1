import assert from 'node:assert/strict';import fs from 'node:fs';import test from 'node:test';import {evaluateCustody} from './task-0074-evidence-custody.mjs';const good=()=>JSON.parse(fs.readFileSync('contracts/task-0074-evidence-custody.json'));test('bound custody passes',()=>assert.equal(evaluateCustody({index:good()}).status,'passed'));for(const m of [x=>x.items[0].timestamp='2030-02-30T00:00:00Z',x=>x.items[0].owner=' ',x=>x.items[0].retention_days=1.5,x=>x.items[0].lineage=[{}],x=>x.items[0].sha256='b'.repeat(64)])test('strict custody fails closed',()=>{const x=good();m(x);assert.equal(evaluateCustody({index:x}).status,'failed')});

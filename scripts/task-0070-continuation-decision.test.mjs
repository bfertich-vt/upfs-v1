import assert from 'node:assert/strict';import fs from 'node:fs';import test from 'node:test';import {evaluateContinuation} from './task-0070-continuation-decision.mjs';const good=()=>JSON.parse(fs.readFileSync('contracts/task-0070-continuation-decision.json'));test('exact predecessor contracts bind',()=>assert.equal(evaluateContinuation({decision:good()}).status,'passed'));for(const m of [x=>x.observation_contract='evidence://generic',x=>x.readiness_contract='contracts/other.json',x=>x.digest='bad'])test('unbound predecessor fails closed',()=>{const x=good();m(x);assert.equal(evaluateContinuation({decision:x}).status,'failed')});

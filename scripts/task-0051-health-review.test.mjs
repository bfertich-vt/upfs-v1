import test from 'node:test';import assert from 'node:assert/strict';import {evaluateHealth} from './task-0051-health-review.mjs';const slo={availability:.995,latency_ms:500,freshness_minutes:5,lag_minutes:5};const good=()=>({availability:1,latency_ms:100,freshness_minutes:1,lag_minutes:1,error_budget_remaining:.5,escalation_ref:'escalate://1',review_ref:'review://1'});test('health review passes',()=>assert.equal(evaluateHealth({metrics:good(),slo}).status,'passed'));for(const m of [x=>{x.availability=.9},x=>{x.latency_ms=600},x=>{x.freshness_minutes=9},x=>{x.error_budget_remaining=-1},x=>{x.error_budget_remaining=0;x.pause=false},x=>{x.review_ref=''}])test('health review fails closed',()=>{const x=good();m(x);assert.equal(evaluateHealth({metrics:x,slo}).status,'failed');});

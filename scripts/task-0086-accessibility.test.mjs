import assert from'node:assert/strict';import crypto from'node:crypto';import fs from'node:fs';import test from'node:test';import{evaluateAccessibility}from'./task-0086-accessibility.mjs';const g=()=>JSON.parse(fs.readFileSync('contracts/task-0086-accessibility.json')),r=x=>x.digest=crypto.createHash('sha256').update(JSON.stringify({scope:x.scope,source_refs:x.source_refs,evidence:x.evidence,state:x.state,deployment:x.deployment})).digest('hex');test('accessibility passes',()=>assert.equal(evaluateAccessibility({contract:g()}).status,'passed'));for(const m of[x=>x.source_refs.pop(),x=>{x.evidence.focus=false;r(x)},x=>{x.evidence.unavailable_states=false;r(x)},x=>{x.deployment='performed';r(x)},x=>x.digest='bad'])test('accessibility fails closed',()=>{const x=g();m(x);assert.equal(evaluateAccessibility({contract:x}).status,'failed')});

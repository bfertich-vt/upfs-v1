import crypto from'node:crypto';import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),sources=['docs/MASTER_PLAN.md','docs/compliance/pilot-go-no-go.md'],tasks=Array.from({length:38},(_,i)=>`TASK-${String(i+71).padStart(4,'0')}`),hash=x=>crypto.createHash('sha256').update(JSON.stringify(x)).digest('hex'),canon=x=>({scope:x?.scope,source_refs:x?.source_refs,evidence:x?.evidence,deployment:x?.deployment});export function evaluateRcEvidenceIndex({contract,synthetic_only=true}={}){const e=contract?.evidence,c=[],a=(n,p)=>c.push({name:n,status:p?'passed':'failed'});a('boundary',synthetic_only&&contract?.deployment==='not-performed');a('source',Array.isArray(contract?.source_refs)&&JSON.stringify(contract.source_refs)===JSON.stringify(sources)&&sources.every(x=>fs.existsSync(path.join(root,x))));a('index',contract?.schema_version==='upfs.rc-evidence-index.v1'&&contract?.scope?.tenant_id==='synthetic-tenant'&&contract?.scope?.environment_id==='pilot'&&typeof e?.ref==='string'&&e.ref.startsWith('evidence://')&&JSON.stringify(e?.tasks)===JSON.stringify(tasks)&&['artifacts','tests','controls','owners'].every(x=>e?.[x]==='indexed')&&e?.risks==='open'&&e?.external_prerequisites==='open'&&e?.state==='blocked'&&e?.replay==='replayed'&&e?.conflict==='rejected');a('integrity',contract?.digest===hash(canon(contract)));return{status:c.every(x=>x.status==='passed')?'passed':'failed',decision:'NO-GO_EXTERNAL_PREREQUISITES',checks:c}}

import assert from'node:assert/strict';import crypto from'node:crypto';import fs from'node:fs';import test from'node:test';import{evaluateBackupKeys}from'./task-0092-backup-keys.mjs';const g=()=>JSON.parse(fs.readFileSync('contracts/task-0092-backup-keys.json')),r=x=>x.digest=crypto.createHash('sha256').update(JSON.stringify({scope:x.scope,source_refs:x.source_refs,evidence:x.evidence,state:x.state,deployment:x.deployment})).digest('hex');test('backup keys pass',()=>assert.equal(evaluateBackupKeys({contract:g()}).status,'passed'));for(const m of[x=>{x.evidence.encrypted=false;r(x)},x=>{x.evidence.key_ref='bad';r(x)},x=>{x.evidence.rotation=false;r(x)},x=>{x.evidence.restore_usable=false;r(x)},x=>{x.evidence.revocation=false;r(x)},x=>{x.deployment='performed';r(x)},x=>x.digest='bad'])test('backup keys fail closed',()=>{const x=g();m(x);assert.equal(evaluateBackupKeys({contract:x}).status,'failed')});

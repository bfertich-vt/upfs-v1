import crypto from'node:crypto';import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const h=x=>crypto.createHash('sha256').update(JSON.stringify(x)).digest('hex');const data=x=>({scope:x.scope,source_refs:x.source_refs,sdk:x.sdk,consumers:x.consumers,state:x.state,deployment:x.deployment});export function evaluateSdkConsumer({contract,synthetic_only=true}={}){const c=contract,q=[];const a=(name,ok)=>q.push({name,status:ok?'passed':'failed'}),src=['specs/13_docs/documentation_platform.md','specs/05_apis/api_standards.md'],cs=Array.isArray(c?.consumers)?c.consumers:[];a('boundary',synthetic_only&&c?.state==='blocked'&&c?.deployment==='not-performed');a('source',JSON.stringify(c?.source_refs)===JSON.stringify(src)&&src.every(x=>fs.existsSync(path.join(root,x))));a('sdk',c?.schema_version==='upfs.sdk-consumer.v1'&&c?.scope?.tenant&&c?.scope?.environment&&c?.sdk?.ref?.startsWith('evidence://')&&c.sdk.generated&&c.sdk.examples_valid&&c.sdk.version);a('consumers',cs.length>0&&new Set(cs.map(x=>x.name)).size===cs.length&&cs.every(x=>x.name&&x.version===c.sdk.version&&x.pagination===true&&x.errors===true&&['oauth-oidc','workload-identity'].includes(x.auth)&&x.permissions===true));a('integrity',c?.digest===h(data(c)));return{status:q.every(x=>x.status==='passed')?'passed':'failed',decision:'NO-GO_EXTERNAL_PREREQUISITES',checks:q}}if(process.argv[1]&&path.resolve(process.argv[1])===path.resolve(fileURLToPath(import.meta.url))){const c=JSON.parse(fs.readFileSync(path.join(root,'contracts/task-0085-sdk-consumer.json')));fs.writeFileSync(path.join(root,'artifacts/task-0085-sdk-consumer.json'),JSON.stringify(evaluateSdkConsumer({contract:c}),null,2)+'\n')}

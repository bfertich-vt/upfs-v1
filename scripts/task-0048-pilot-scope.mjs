import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');export function evaluateScope({manifest,synthetic_only=true,deployment='not-performed'}={}){const c=[];const a=(n,o)=>c.push({name:n,status:o?'passed':'failed'});const b=v=>typeof v==='string'&&v.trim().length>0;a('boundary',synthetic_only&&deployment==='not-performed');a('version',b(manifest?.version)&&b(manifest?.immutable_ref));a('cohorts',Array.isArray(manifest?.cohorts)&&manifest.cohorts.length>0&&manifest.cohorts.every(b));a('regions',Array.isArray(manifest?.regions)&&manifest.regions.length>0&&manifest.regions.every(b));a('environments',Array.isArray(manifest?.environments)&&manifest.environments.includes('pilot'));a('tenant-scope',b(manifest?.tenant_scope_ref));a('prohibited-data',Array.isArray(manifest?.prohibited_data)&&manifest.prohibited_data.length>0);a('retention',Number.isInteger(manifest?.retention_days)&&manifest.retention_days>0);a('pause',b(manifest?.pause_boundary_ref));return{schema_version:'upfs.pilot-scope-report.v1',status:c.every(x=>x.status==='passed')?'passed':'failed',decision:'NO-GO_EXTERNAL_PREREQUISITES',synthetic_only,deployment,checks:c};}if(process.argv[1]&&path.resolve(process.argv[1])===path.resolve(fileURLToPath(import.meta.url))){const r=evaluateScope();fs.writeFileSync(path.join(root,'artifacts/task-0048-pilot-scope-report.json'),JSON.stringify(r,null,2)+'\n');console.log(`TASK-0048 scope: ${r.decision}`);}

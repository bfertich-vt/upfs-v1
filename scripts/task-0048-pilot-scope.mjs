import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');export function evaluateScope({manifest,synthetic_only=true,deployment='not-performed'}={}){const c=[];const a=(n,o)=>c.push({name:n,status:o?'passed':'failed'});const b=v=>typeof v==='string'&&v.trim().length>0;const unique=a=>Array.isArray(a)&&a.length>0&&a.every(b)&&new Set(a).size===a.length;a('boundary',synthetic_only&&deployment==='not-performed');a('version',b(manifest?.version)&&b(manifest?.immutable_ref));a('cohorts',unique(manifest?.cohorts));a('regions',unique(manifest?.regions));a('environments',unique(manifest?.environments)&&manifest.environments.includes('pilot'));a('tenant-scope',b(manifest?.tenant_scope_ref));a('prohibited-data',unique(manifest?.prohibited_data));a('retention',Number.isInteger(manifest?.retention_days)&&manifest.retention_days>0);a('pause',b(manifest?.pause_boundary_ref));return{schema_version:'upfs.pilot-scope-report.v1',status:c.every(x=>x.status==='passed')?'passed':'failed',decision:'NO-GO_EXTERNAL_PREREQUISITES',synthetic_only,deployment,checks:c};}if(process.argv[1]&&path.resolve(process.argv[1])===path.resolve(fileURLToPath(import.meta.url))){const manifest=JSON.parse(fs.readFileSync(path.join(root,'contracts/task-0048-pilot-scope.json')));const r=evaluateScope({manifest});fs.writeFileSync(path.join(root,'artifacts/task-0048-pilot-scope-report.json'),JSON.stringify(r,null,2)+'\n');console.log(`TASK-0048 scope: ${r.decision}`);}

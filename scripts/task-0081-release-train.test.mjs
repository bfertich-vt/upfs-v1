import assert from 'node:assert/strict';import fs from 'node:fs';import test from 'node:test';import {evaluateReleaseTrain} from './task-0081-release-train.mjs';const good=()=>JSON.parse(fs.readFileSync('contracts/task-0081-release-train.json'));test('release train passes',()=>assert.equal(evaluateReleaseTrain({board:good()}).status,'passed'));for(const m of [x=>x.gates=[],x=>x.rollback_ref='bad',x=>x.state='ready',x=>x.conflict_rejected=false])test('release train fails closed',()=>{const x=good();m(x);assert.equal(evaluateReleaseTrain({board:x}).status,'failed')});

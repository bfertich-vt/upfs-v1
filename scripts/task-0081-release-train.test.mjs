import assert from 'node:assert/strict';import crypto from 'node:crypto';import fs from 'node:fs';import test from 'node:test';import {evaluateReleaseTrain} from './task-0081-release-train.mjs';const good=()=>JSON.parse(fs.readFileSync('contracts/task-0081-release-train.json'));const rehash=x=>{x.digest=crypto.createHash('sha256').update(JSON.stringify({scope:x.scope,source_refs:x.source_refs,candidate:x.candidate,gates:x.gates,owners:x.owners,approvals:x.approvals,artifact:x.artifact,rollback:x.rollback,blockers:x.blockers,state:x.state,replay:x.replay,conflict:x.conflict,deployment:x.deployment})).digest('hex')};test('release train passes',()=>assert.equal(evaluateReleaseTrain({board:good()}).status,'passed'));for(const m of [x=>x.source_refs.pop(),x=>{x.gates[0].tenant='other';rehash(x)},x=>{x.artifact.ref='bad';rehash(x)},x=>{x.owners=[];rehash(x)},x=>{x.approvals=[];rehash(x)},x=>{x.blockers=[];rehash(x)},x=>{x.conflict.rejected=false;rehash(x)},x=>x.digest='bad'])test('release train fails closed',()=>{const x=good();m(x);assert.equal(evaluateReleaseTrain({board:x}).status,'failed')});

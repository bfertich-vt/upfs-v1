import assert from 'node:assert/strict';import fs from 'node:fs';import test from 'node:test';import {evaluateDeprovisioning} from './task-0080-provider-deprovisioning.mjs';const good=()=>JSON.parse(fs.readFileSync('contracts/task-0080-provider-deprovisioning.json'));test('deprovisioning passes',()=>assert.equal(evaluateDeprovisioning({record:good()}).status,'passed'));for(const m of [x=>x.revocation_verified=false,x=>x.evidence_retained=false,x=>x.conflict_rejected=false,x=>x.deployment='performed'])test('deprovisioning fails closed',()=>{const x=good();m(x);assert.equal(evaluateDeprovisioning({record:x}).status,'failed')});

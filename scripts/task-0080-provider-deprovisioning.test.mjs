import assert from 'node:assert/strict';import fs from 'node:fs';import test from 'node:test';import {evaluateDeprovisioning} from './task-0080-provider-deprovisioning.mjs';const good=()=>JSON.parse(fs.readFileSync('contracts/task-0080-provider-deprovisioning.json'));test('scoped provider deprovisioning passes',()=>assert.equal(evaluateDeprovisioning({record:good()}).status,'passed'));for(const m of [x=>x.approvals[0].tenant_id='other',x=>x.approvals.pop(),x=>x.transitions.pop(),x=>x.replay.result='new',x=>x.conflict.rejected=false,x=>x.digest='bad'])test('scope quorum state replay and integrity fail closed',()=>{const x=good();m(x);assert.equal(evaluateDeprovisioning({record:x}).status,'failed')});

import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');export function evaluateSupport({request,synthetic_only=true,deployment='not-performed',now=new Date()}={}){const c=[];const a=(n,o)=>c.push({name:n,status:o?'passed':'failed'});const b=v=>typeof v==='string'&&v.trim().length>0;a('boundary',synthetic_only&&deployment==='not-performed');a('case',b(request?.case_id)&&b(request?.reason));a('approval',request?.approval?.approved===true&&b(request.approval.approver));a('step-up',request?.step_up_verified===true);a('scope',b(request?.tenant_scope_ref)&&Array.isArray(request?.allowed_fields)&&request.allowed_fields.length>0);a('expiry',b(request?.expires_at)&&Date.parse(request.expires_at)>now.getTime());a('redaction',request?.redacted===true);a('audit',b(request?.audit_ref));return{schema_version:'upfs.support-access-report.v1',status:c.every(x=>x.status==='passed')?'passed':'failed',decision:'NO-GO_EXTERNAL_PREREQUISITES',synthetic_only,deployment,checks:c};}if(process.argv[1]&&path.resolve(process.argv[1])===path.resolve(fileURLToPath(import.meta.url))){const r=evaluateSupport();fs.writeFileSync(path.join(root,'artifacts/task-0053-support-access-report.json'),JSON.stringify(r,null,2)+'\n');console.log(`TASK-0053 support: ${r.decision}`);}

import assert from 'node:assert/strict';import fs from 'node:fs';import test from 'node:test';import {evaluateOffboarding} from './task-0079-tenant-offboarding.mjs';const good=()=>JSON.parse(fs.readFileSync('contracts/task-0079-tenant-offboarding.json'));test('offboarding passes',()=>assert.equal(evaluateOffboarding({rehearsal:good()}).status,'passed'));for(const m of [x=>x.access_revoked=false,x=>x.evidence_preserved=false,x=>x.cross_tenant_leakage=true,x=>x.conflict_rejected=false])test('offboarding fails closed',()=>{const x=good();m(x);assert.equal(evaluateOffboarding({rehearsal:x}).status,'failed')});

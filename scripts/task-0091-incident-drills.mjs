import crypto from'node:crypto';import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),h=x=>crypto.createHash('sha256').update(JSON.stringify(x)).digest('hex'),d=x=>({scope:x?.scope,source_refs:x?.source_refs,drills:x?.drills,state:x?.state,deployment:x?.deployment}),need=['health','security','dependency','tenant-isolation','data-quality','recovery','release'];export function evaluateIncidentDrills({contract,synthetic_only=true}={}){const c=contract,q=[],a=(n,o)=>q.push({name:n,status:o?'passed':'failed'}),s=['docs/runbooks/controlled-pilot-runbook.md','specs/12_testing/test_strategy.md'];a('boundary',synthetic_only&&c?.state==='blocked'&&c?.deployment==='not-performed');a('source',Array.isArray(c?.source_refs)&&JSON.stringify(c.source_refs)===JSON.stringify(s)&&s.every(x=>fs.existsSync(path.join(root,x))));a('coverage',c?.schema_version==='upfs.incident-drills.v1'&&c?.scope?.tenant_id?.trim()===c.scope.tenant_id&&c?.scope?.environment_id?.trim()===c.scope.environment_id&&Array.isArray(c?.drills)&&c.drills.length===need.length&&new Set(c.drills).size===need.length&&need.every(x=>c.drills.includes(x)));a('integrity',c?.digest===h(d(c)));return{status:q.every(x=>x.status==='passed')?'passed':'failed',decision:'NO-GO_EXTERNAL_PREREQUISITES',checks:q}}

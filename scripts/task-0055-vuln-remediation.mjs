import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');export function evaluateVuln({scan,synthetic_only=true,deployment='not-performed'}={}){const c=[];const a=(n,o)=>c.push({name:n,status:o?'passed':'failed'});const b=v=>typeof v==='string'&&v.trim().length>0;a('boundary',synthetic_only&&deployment==='not-performed');a('pinned',b(scan?.tool_version)&&b(scan?.manifest_ref));a('threshold',Number.isInteger(scan?.critical)&&scan.critical===0&&Number.isInteger(scan?.high)&&scan.high===0);a('ownership',Array.isArray(scan?.findings)&&scan.findings.every(x=>b(x.owner)&&b(x.deadline)));a('exceptions',Array.isArray(scan?.exceptions));a('retest',scan?.retest_passed===true&&b(scan?.retest_ref));a('audit',b(scan?.audit_ref));return{schema_version:'upfs.vulnerability-report.v1',status:c.every(x=>x.status==='passed')?'passed':'failed',decision:'NO-GO_EXTERNAL_PREREQUISITES',synthetic_only,deployment,checks:c};}if(process.argv[1]&&path.resolve(process.argv[1])===path.resolve(fileURLToPath(import.meta.url))){const r=evaluateVuln();fs.writeFileSync(path.join(root,'artifacts/task-0055-vulnerability-report.json'),JSON.stringify(r,null,2)+'\n');console.log(`TASK-0055 vulnerability: ${r.decision}`);}

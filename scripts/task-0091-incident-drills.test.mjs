import assert from'node:assert/strict';import crypto from'node:crypto';import fs from'node:fs';import test from'node:test';import{evaluateIncidentDrills}from'./task-0091-incident-drills.mjs';const g=()=>JSON.parse(fs.readFileSync('contracts/task-0091-incident-drills.json')),r=x=>x.digest=crypto.createHash('sha256').update(JSON.stringify({scope:x.scope,source_refs:x.source_refs,drills:x.drills,state:x.state,deployment:x.deployment})).digest('hex');test('incident drills pass',()=>assert.equal(evaluateIncidentDrills({contract:g()}).status,'passed'));for(const m of[x=>{x.drills.pop();r(x)},x=>{x.drills[0]='security';r(x)},x=>{x.scope.tenant_id=' ';r(x)},x=>{x.deployment='performed';r(x)},x=>x.digest='bad'])test('incident drills fail closed',()=>{const x=g();m(x);assert.equal(evaluateIncidentDrills({contract:x}).status,'failed')});

import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');export function evaluateCustody({index,synthetic_only=true}={}){const c=[];const a=(n,o)=>c.push({name:n,status:o?'passed':'failed'});a('boundary',synthetic_only&&index?.deployment==='not-performed');a('schema',index?.schema_version==='upfs.evidence-custody.v1');a('custody',Array.isArray(index?.items)&&index.items.length&&index.items.every(x=>x.id&&x.owner&&/^[a-f0-9]{64}$/.test(x.sha256||'')&&/^\d{4}-\d\d-\d\dT/.test(x.timestamp||'')&&x.retention_days>0&&x.access==='restricted'&&Array.isArray(x.lineage)&&x.lineage.length&&x.disposal==='approved'&&String(x.evidence_ref).startsWith('evidence://')));return{status:c.every(x=>x.status==='passed')?'passed':'failed',decision:'NO-GO_EXTERNAL_PREREQUISITES',checks:c}}if(process.argv[1]&&path.resolve(process.argv[1])===path.resolve(fileURLToPath(import.meta.url))){const index=JSON.parse(fs.readFileSync(path.join(root,'contracts/task-0074-evidence-custody.json')));fs.writeFileSync(path.join(root,'artifacts/task-0074-evidence-custody.json'),JSON.stringify(evaluateCustody({index}),null,2)+'\n')}

import crypto from'node:crypto';import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),sources=['specs/13_docs/documentation_platform.md','specs/05_apis/api_standards.md'],hash=x=>crypto.createHash('sha256').update(JSON.stringify(x)).digest('hex'),canonical=x=>({scope:x?.scope,source_refs:x?.source_refs,evidence:x?.evidence,deployment:x?.deployment});export function evaluatePublicDocsDrift({contract,synthetic_only=true}={}){const e=contract?.evidence,c=[],a=(n,p)=>c.push({name:n,status:p?'passed':'failed'});a('boundary',synthetic_only&&contract?.deployment==='not-performed');a('source',Array.isArray(contract?.source_refs)&&JSON.stringify(contract.source_refs)===JSON.stringify(sources)&&sources.every(x=>fs.existsSync(path.join(root,x))));a('refresh',contract?.schema_version==='upfs.public-docs-drift.v1'&&contract?.scope?.tenant_id==='synthetic-tenant'&&contract?.scope?.environment_id==='pilot'&&typeof e?.ref==='string'&&e.ref.startsWith('evidence://')&&['docs','examples','sdks','contracts','versions','permissions','errors','migrations'].every(x=>e?.[x]==='synchronized')&&e?.state==='blocked'&&e?.replay==='replayed'&&e?.conflict==='rejected');a('integrity',contract?.digest===hash(canonical(contract)));return{status:c.every(x=>x.status==='passed')?'passed':'failed',decision:'NO-GO_EXTERNAL_PREREQUISITES',checks:c}}

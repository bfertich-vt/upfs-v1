import assert from 'node:assert/strict';import fs from 'node:fs';import test from 'node:test';import {evaluateAuditEvidence} from './task-0075-audit-evidence.mjs';const good=()=>JSON.parse(fs.readFileSync('contracts/task-0075-audit-evidence.json'));test('structured audit passes',()=>assert.equal(evaluateAuditEvidence({review:good()}).status,'passed'));for(const m of [x=>x.records[0].actor.id='tampered',x=>x.records[0].timestamp='2030-02-30T00:00:00Z',x=>x.records[0].environment_scope='',x=>x.digest='bad'])test('audit tampering fails closed',()=>{const x=good();m(x);assert.equal(evaluateAuditEvidence({review:x}).status,'failed')});

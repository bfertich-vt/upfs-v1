import assert from 'node:assert/strict';import fs from 'node:fs';import test from 'node:test';import {evaluateAuditEvidence} from './task-0075-audit-evidence.mjs';const good=()=>JSON.parse(fs.readFileSync('contracts/task-0075-audit-evidence.json'));test('audit evidence passes',()=>assert.equal(evaluateAuditEvidence({review:good()}).status,'passed'));for(const m of [x=>x.records[0].immutable=false,x=>x.records[0].tenant_scope='',x=>x.records[0].hash='bad',x=>x.records=[]])test('audit tampering fails closed',()=>{const x=good();m(x);assert.equal(evaluateAuditEvidence({review:x}).status,'failed')});

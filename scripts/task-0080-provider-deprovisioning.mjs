import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');export function evaluateDeprovisioning({record,synthetic_only=true}={}){const c=[];const a=(n,o)=>c.push({name:n,status:o?'passed':'failed'});a('boundary',synthetic_only&&record?.deployment==='not-performed');a('lifecycle',record?.schema_version==='upfs.provider-deprovisioning.v1'&&record.provider&&record.state==='deprovisioned'&&record.binding_verified&&record.rotation_verified&&record.revocation_verified&&record.evidence_retained&&record.approval?.actor&&record.approval.approved&&record.replay==='replayed'&&record.conflict_rejected&&String(record.audit_ref).startsWith('evidence://')&&record.digest);return{status:c.every(x=>x.status==='passed')?'passed':'failed',decision:'NO-GO_EXTERNAL_PREREQUISITES',checks:c}}if(process.argv[1]&&path.resolve(process.argv[1])===path.resolve(fileURLToPath(import.meta.url))){const record=JSON.parse(fs.readFileSync(path.join(root,'contracts/task-0080-provider-deprovisioning.json')));fs.writeFileSync(path.join(root,'artifacts/task-0080-provider-deprovisioning.json'),JSON.stringify(evaluateDeprovisioning({record}),null,2)+'\n')}

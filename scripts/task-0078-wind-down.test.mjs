import assert from 'node:assert/strict';import fs from 'node:fs';import test from 'node:test';import {evaluateWindDown} from './task-0078-wind-down.mjs';const good=()=>JSON.parse(fs.readFileSync('contracts/task-0078-wind-down.json'));test('wind down passes',()=>assert.equal(evaluateWindDown({workflow:good()}).status,'passed'));for(const m of [x=>x.protections.audit=false,x=>x.transition.approved=false,x=>x.replay.result='new',x=>x.conflict.rejected=false])test('wind down fails closed',()=>{const x=good();m(x);assert.equal(evaluateWindDown({workflow:x}).status,'failed')});

import assert from 'node:assert/strict';import fs from 'node:fs';import test from 'node:test';import {evaluateWindDown} from './task-0078-wind-down.mjs';const good=()=>JSON.parse(fs.readFileSync('contracts/task-0078-wind-down.json'));test('wind down passes',()=>assert.equal(evaluateWindDown({workflow:good()}).status,'passed'));for(const m of [x=>delete x.protections.audit,x=>x.protections.extra=true,x=>x.protections.data='true',x=>x.transition.key=' ',x=>x.deployment='performed'])test('wind down fails strict mutation',()=>{const x=good();m(x);assert.equal(evaluateWindDown({workflow:x}).status,'failed')});

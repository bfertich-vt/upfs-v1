# Infrastructure

Local containers are development-only. Production infrastructure must be defined as code, policy checked, encrypted, region/tenant aware, observable, and promoted through the controlled release pipeline.

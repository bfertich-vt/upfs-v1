# TASK-0026 — Public documentation and SDK synchronization

Source: `specs/13_docs/documentation_platform.md` and `specs/05_apis/api_standards.md`.

`scripts/generate-public-docs.mjs` deterministically generates versioned API reference and TypeScript SDK route artifacts from OpenAPI, AsyncAPI, and JSON Schema contracts. `--check` fails closed on missing or stale output. Generated docs include routes, operation IDs, permissions, responses, events, schemas, and synthetic-only examples.

Evidence: run `node scripts/generate-public-docs.mjs`, `node scripts/generate-public-docs.mjs --check`, and `npm test`. No credentials, deployment, or customer data used.

# TASK-0014 handoff

- Scope: reproducible migration, security, recovery, and environment-sized load rehearsals.
- Implementation: `scripts/task-0014-rehearsal.mjs` runs ordered migration/RLS and contract checks, synthetic BOLA/secret scans, checkpoint/outbox recovery checks, and a bounded 25,000-operation load gate. When `UPFS_DATABASE_URL` and `psql` are present it applies each migration in a disposable transaction with `ON_ERROR_STOP`; otherwise the live gate is explicitly recorded as `skipped` and the process never claims live evidence.
- Evidence: `scripts/task-0014-rehearsal.test.mjs`; report artifact `artifacts/task-0014-rehearsal-report.json`; synthetic-only fixtures. Run `node scripts/task-0014-rehearsal.mjs` and `node --test scripts/task-0014-rehearsal.test.mjs`.
- Security and tenant isolation: checks forced RLS/policies in every migration and denies a synthetic cross-tenant lookup. No credentials or customer data are used; PostgreSQL probe output is not persisted.
- Recovery and rollback: migrations are applied one at a time with single-transaction mode in a disposable database. Failed migrations stop immediately. Production rollback remains corrective-forward per migration policy; no deployment or external database mutation is performed without an explicitly supplied disposable URL.
- Current external evidence: this environment has no Docker/`psql` prerequisite, so live PostgreSQL migration, restore, and RLS evidence is not claimed. The report is fail-closed and marks that prerequisite skipped.
- Known follow-up: run the same command against an isolated disposable PostgreSQL instance before pilot authorization; retain the generated report with release evidence.

# TASK-0020 handoff

- Task and scope: formal controlled-pilot go/no-go authorization package; no deployment, external contact, certification, attestation, credentials, or customer data.
- Files changed: `scripts/task-0020-authorization.mjs`, its tests, and this handoff.
- Evidence behavior: the evaluator builds a versioned evidence index for native PostgreSQL, release, managed controls, pilot rehearsal, controls, and runbook artifacts. It records SHA-256 digests, verifies the immutable release manifest and every retained entry, optionally compares an expected evidence manifest, and enforces valid, bounded freshness for executable evidence reports. It requires passed native evidence and non-deploying synthetic boundaries and fails closed on missing or malformed evidence.
- Authorization behavior: named dated approvals for release, security, data, incident, and external deployment are mandatory. Each approval requires identity, scope, reason, signature/reference, strict RFC3339 issue/expiry dates, and future expiry; external deployment additionally requires deployment scope and dual control. The default and incomplete package is `NO-GO`; only an explicit `GO` with every gate and approval can produce `GO`. Production launch, certification, and attestation are explicitly not claimed.
- Security analysis: reports retain no credentials or financial data; evidence paths are repository-relative and digested. Approval metadata is an external authorization record, not synthesized by the evaluator.
- Tests and commands: `node --test scripts/task-0020-authorization.test.mjs`; full `npm test`; `scripts/validate.ps1`; `git diff --check`.
- Results: complete fixture reaches GO; missing native evidence, non-native/tampered/stale evidence, manifest drift, malformed approvals, and missing approvals remain NO-GO. Direct CLI invocation is covered with a Windows-safe `fileURLToPath` main guard and report-output assertion.
- Rollback/corrective-forward: additive code/docs revert through protected review; any pilot action remains externally approval-gated and uses corrective-forward recovery.
- Known risks: actual approval signatures, managed observability, and pilot deployment remain external prerequisites; this package does not authorize or perform them.

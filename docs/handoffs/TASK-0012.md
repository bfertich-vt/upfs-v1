# TASK-0012 handoff

- Task and scope: PostgreSQL persistence boundary, tenant-scoped domain migration, RLS, transaction/rollback adapter.
- Files changed: `infra/migrations/001_identity_tenant_rls.sql`, `infra/migrations/002_persistence_domain_rls.sql`, `services/postgres-persistence.mjs`, `services/postgres-persistence.test.mjs`, `services/production-runtime.mjs`, and composition/rehearsal checks.
- Contracts/migrations: Adds raw evidence, canonical transactions, projection metadata, policies, workflows, and append-only audit tables. Every tenant table has a tenant key, indexes, and FORCE RLS with fail-closed `app.tenant_id` policy.
- Security and tenant-isolation analysis: Production adapter requires injected pg-compatible pool and explicit tenant scope, sets transaction-local RLS context after BEGIN, and rolls back on all callback errors. No client-supplied scope is trusted outside the caller's authorization boundary. Missing pool/configuration fails closed.
- Audit/evidence behavior: `insertAudit` writes append-only, allowlisted scalar metadata inside a tenant-scoped transaction and rejects raw content, prompts, credentials, secrets, tokens, and financial payloads.
- Production composition: each injected production service must expose `repository` equal to the injected PostgreSQL adapter; `mode: "reference-test"` is explicit and limited to synthetic rehearsal/reference services.
- Tests and commands run: 81/81 tests passed; `scripts/validate.ps1`; `scripts/production-composition-check.mjs`; `git diff --check`.
- Results: Adapter tests cover missing dependency, commit/context, rollback, and tenant-scoped audit. Disposable PostgreSQL integration is not claimed unless `UPFS_DATABASE_URL` and a reachable PostgreSQL service are supplied.
- Rollback/corrective-forward plan: Revert migration 002 only before dependent data exists; otherwise use a reviewed corrective-forward migration. Adapter is additive and can be disabled by composition until credentials are provisioned.
- Documentation updated: This handoff.
- Known risks and follow-ups: A reachable disposable PostgreSQL service and `UPFS_DATABASE_URL` are still required before claiming live migration/RLS/rollback evidence; TASK-0013 must add durable outbox/checkpoint implementations and the live integration harness.

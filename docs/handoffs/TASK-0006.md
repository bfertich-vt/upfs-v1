# TASK-0006 handoff

- Task and scope: Rebuildable in-memory OpenSearch-like transaction projection, idempotent/out-of-order-safe event consumer, tenant-scoped reconciliation/rebuild, and bounded cursor search API.
- Files changed: `services/transaction-projection.mjs`, `services/transaction-projection.test.mjs`, `contracts/schemas/transaction-search-response.schema.json`, `contracts/openapi/projection-search-api.yaml`.
- Contracts/migrations: Versioned projection documents (`projection_version: 1.0.0`) and stable search envelope. No database migration; production boundary is PostgreSQL canonical state plus transactional outbox feeding versioned OpenSearch aliases.
- Security and tenant-isolation analysis: Verified actor and authorization-derived tenant scope are required for consume/reconcile/rebuild/search. Search never returns documents from another tenant and cursor binds tenant and query hash. Limits and query/cursor inputs are bounded. Projection internals (source hashes/timestamps) are not returned. Negative unauthenticated, forbidden, cursor, and cross-tenant tests included.
- Audit/evidence behavior: Consumer, reconciliation, and rebuild operations append metadata-only audit entries with IDs, scope, actor, event/version, and counts; no raw descriptions, artifacts, credentials, or financial payloads are logged. Canonical evidence references are preserved in documents.
- Tests and commands run: `node --test services/*.test.mjs`; `scripts/validate.ps1`; `git diff --check`.
- Results: Projection tests cover idempotency, event conflict, out-of-order replay, stable cursor pagination, authorization/BOLA denial, deterministic parity, drift detection, and rebuild.
- Remediation evidence: consume now authorizes the transaction tenant, same-version payload changes conflict, search cursors are HMAC-protected and trimmed-empty queries are rejected, search response items use a projection-specific contract, reconcile watermarks cannot exceed the observed checkpoint, rebuild watermarks are validated monotonically against existing/source versions, and rebuild clears tenant event-idempotency state. Tests: 48/48 passing; validation and diff checks pass.
- Rollback/corrective-forward plan: Remove/revert projection consumer and route reads to canonical API; rebuild any new OpenSearch index from PostgreSQL/outbox after correction, then atomically promote an alias. Never delete canonical facts or raw evidence.
- Documentation updated: This handoff and projection-search OpenAPI/schema contracts.
- Known risks and follow-ups: Reference implementation uses in-memory maps; production requires PostgreSQL outbox offsets, durable consumer checkpoints, OpenSearch versioned aliases, bulk backfill throttling, failure injection, metrics/alerts, and a real authorization policy engine. Search matching is intentionally simple until governed query DSL is introduced.

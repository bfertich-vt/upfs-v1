# Handoff report

- Task and scope: Durable transactional outbox, monotonic checkpoints, and recovery worker contracts.
- Files changed: `services/transaction-outbox.mjs`, `services/transaction-outbox.test.mjs`, `infra/migrations/003_outbox_checkpoints.sql`.
- Contracts/migrations: PostgreSQL outbox supports tenant-scoped append with idempotency, SKIP LOCKED lease claims, ownership-checked acknowledge/fail, bounded retries and dead-letter; checkpoints use monotonic `GREATEST` updates. RLS is forced on both tables.
- Security and tenant-isolation analysis: Every adapter operation requires an explicit authorization-derived tenant scope and delegates through the PostgreSQL transaction boundary; event ownership is checked on ack/fail. No in-memory production fallback exists and payload fixtures are synthetic.
- Audit/evidence behavior: Event payloads remain durable for replay; consumers must be idempotent and checkpoint advances only after acknowledgement. Failure leaves event retryable or dead-lettered.
- Tests and commands run: `node --test services/transaction-outbox.test.mjs`; full repository tests; `scripts/validate.ps1`; `git diff --check`.
- Results: Synthetic fake-pg tests cover idempotency, claim ownership, retries/dead-letter, monotonic checkpoints, recovery failure/restart behavior, tenant scope, and missing dependency fail-closed behavior. No live PostgreSQL or production credentials claimed.
- Rollback/corrective-forward plan: Apply migration 003 forward; before dependent data exists, revert migration in a reviewed maintenance window. Otherwise retain append-only rows and use corrective-forward schema changes.
- Documentation updated: This handoff.
- Known risks and follow-ups: Disposable PostgreSQL migration and crash/restart/load rehearsal remain TASK-0014 prerequisites; live SQL semantics must be exercised against supported PostgreSQL before production use.

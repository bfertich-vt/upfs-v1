# TASK-0009 handoff

- Task and scope: deny-by-default policy evaluation plus API-backed durable workflow and approval reference slice, remediated for fail-closed authorization and durable-state boundaries.
- Files changed: `services/policy-workflow.mjs`, `services/policy-workflow.test.mjs`.
- Contracts/migrations: service methods model `/v1` policy publish/evaluate, workflow create/read/approve/transition semantics. PostgreSQL remains authoritative in production; transactional outbox is required for durable transitions and external effects.
- Security and tenant-isolation analysis: actor issuer/subject and server authorization callback are required; tenant and environment are carried from the authorized boundary and checked on every operation. Missing policy denies. Cross-tenant reads/writes and unauthenticated calls are rejected. LLMs are not involved in policy or approval.
- Audit/evidence behavior: policy decisions include allow/deny, policy version, reason code, obligations, evaluated attributes, and correlation ID. Workflow transitions, approvals, and policy publication append safe audit records; workflow history and an outbox are append-only. Approval reasons/evidence references are bounded, approvals require idempotency keys, and the creator cannot approve their own workflow.
- Tests and commands run: `node --test services/policy-workflow.test.mjs`; full repository tests and `scripts/validate.ps1` run at handoff.
- Results: at commit 7c8772a, in-memory reference passes targeted negative authorization, cross-tenant, operation-scoped replay/idempotency, strict serializable policy rule, dual-control, legal-transition, denial, policy re-evaluation, and export/recovery tests; full suite 62/62 and validation pass.
- Rollback/corrective-forward plan: revert the commit; production persistence must use PostgreSQL transactions and outbox replay. Failed/retryable steps transition through durable retry states; irreversible actions require approved action requests and postcondition verification.
- Documentation updated: this handoff.
- Known risks and follow-ups: replace maps with PostgreSQL repositories/outbox and transactional checkpoint/event persistence, add OpenAPI schemas and worker retry/timeout execution, and add policy publish test/impact/shadow rollout controls before production. The exported state API is a reference seam, not a production durability claim.

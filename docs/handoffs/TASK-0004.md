# TASK-0004 handoff

- Task and scope: Raw evidence intake and quarantine reference vertical slice.
- Files changed: `services/evidence-intake.mjs`, its tests, and `contracts/schemas/raw-evidence.schema.json`.
- Contracts/migrations: JSON Schema for immutable raw observations; no persistence migration (in-memory reference service).
- Security and tenant-isolation analysis: Authenticated actor, server-supplied tenant/environment authorization, deny-by-default scope checks, cross-tenant/environment negative tests, bounded content size, strict fields and timestamps.
- Audit/evidence behavior: Raw content is preserved in the returned immutable observation and never copied into audit entries; audit records contain actor, scope, resource, hash, status. Intake is quarantined before downstream release.
- Tests and commands run: `node --test services/*.test.mjs`; `scripts/validate.ps1`; `git diff --check`.
- Results: All tests and repository validation pass at handoff time.
- Rollback/corrective-forward plan: Remove the service/schema commit; production implementation must replace in-memory maps with PostgreSQL immutable artifact storage and transactional outbox.
- Documentation updated: This handoff.
- Known risks and follow-ups: Malware/content/prompt-injection classifier is an explicit hook and must be replaced by managed scanners before production; API routing, durable storage, retention, and outbox belong to subsequent delivery work.

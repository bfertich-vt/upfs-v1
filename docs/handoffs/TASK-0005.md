# TASK-0005 handoff

- Task and scope: Signed synthetic provider transaction delivery through immutable raw evidence intake and deterministic canonical transaction mapping.
- Files changed: `services/connector-ingestion.mjs`, `services/connector-ingestion.test.mjs`, `contracts/schemas/provider-transaction.schema.json`.
- Contracts/migrations: Provider transaction JSON Schema (`provider-transaction/1.0.0`); no migration (reference adapters remain in-memory).
- Security and tenant-isolation analysis: Connector configuration derives tenant/environment scope; caller-supplied scope is not accepted. OIDC actor and connector authorization are required. HMAC-SHA256 signature covers timestamp, nonce, and exact JSON payload; timestamps are bounded and nonces are replay-protected. Strict payload allowlist prevents mass assignment. Negative tests cover missing actor, forbidden actor, forged signature, stale timestamp, and replay.
- Audit/evidence behavior: Exact provider JSON is preserved as UTF-8 raw evidence with SHA-256 content hash and evidence reference on the canonical transaction. Scanner outcomes remain quarantined; suspicious or blocked evidence cannot reach canonical state. Audit entries contain hashes, IDs, scope, actor, and provider transaction ID, never raw content.
- Tests and commands run: `node --test services/*.test.mjs`; `scripts/validate.ps1`; `git diff --check`.
- Results: All tests and repository validation pass at handoff time (see commit evidence).
- Rollback/corrective-forward plan: Revert this adapter/service commit; already-intaken evidence remains immutable. Production implementation must replace in-memory maps with PostgreSQL transactions/outbox and provider-managed secret verification while retaining signatures, nonce windows, and evidence links.
- Documentation updated: This handoff and provider transaction schema.
- Known risks and follow-ups: HMAC secret lifecycle, rate limiting, durable nonce/idempotency storage, connector health/backoff, provider-specific pagination/webhook envelopes, malware scanning, and transactional outbox are required before production. JSON serialization must remain canonicalized across provider implementations.

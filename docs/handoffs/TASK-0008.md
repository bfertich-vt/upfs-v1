# TASK-0008 handoff

Implemented a governed, API-backed read-only chat vertical slice in `services/governed-chat.mjs` with tenant/actor authorization (fail-closed on policy exceptions), approved canonical record retrieval, strict record/evidence validation, iterative serialized context bounds, trust separation for record content, strict citation verification, deterministic refusal when evidence/citations are absent, synthetic model injection, synchronous compatibility plus `chatAsync`, bounded retrieval/model timeouts, rate limiting, failure handling, and metadata-only audit events for every sensitive attempt/outcome. No tools, writes, or action side effects are exposed. Contract: `contracts/openapi/governed-chat-api.yaml` now specifies request/response/error/rate-limit and no-tools/no-writes behavior; adversarial, async, timeout, bound, schema, and audit tests are in `services/governed-chat.test.mjs`.

Validation evidence (2026-07-31, commit 88b805a): `node --test services/*.test.mjs` (56/56 pass, including strict account_id validation); `powershell -ExecutionPolicy Bypass -File scripts\\validate.ps1` passed; `git diff --check` passed.

Production follow-up: replace the in-memory rate limiter with Redis and wire retrieval to the authenticated projection/canonical API; retain PostgreSQL as authority and preserve audit append-only semantics.

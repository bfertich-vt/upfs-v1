# TASK-0008 handoff

Implemented a governed, API-backed read-only chat vertical slice in `services/governed-chat.mjs` with tenant/actor authorization, approved canonical record retrieval, bounded context, trust separation for record content, strict citation verification, deterministic refusal when evidence/citations are absent, synthetic model injection, rate limiting, failure handling, and audit metadata containing hashes/IDs only. No tools, writes, or action side effects are exposed. Contract: `contracts/openapi/governed-chat-api.yaml`; adversarial and cross-tenant tests: `services/governed-chat.test.mjs`.

Production follow-up: replace the in-memory rate limiter with Redis and wire retrieval to the authenticated projection/canonical API; retain PostgreSQL as authority and preserve audit append-only semantics.

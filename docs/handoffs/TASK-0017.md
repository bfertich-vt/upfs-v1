# Handoff report

- Task and scope: Native PostgreSQL backup, restore, and controlled pilot evidence runner.
- Files changed: `scripts/task-0017-native-postgres.mjs`, `scripts/task-0017-native-postgres.test.mjs`, and this handoff.
- Contracts/migrations: Reuses ordered repository migrations; source and restore targets are always separate and native `pg_dump --format=custom`/`pg_restore --exit-on-error` are used when all prerequisites are present.
- Security and tenant-isolation analysis: URLs are never written to reports; command output is discarded. Native evidence requires explicit disposable URLs and all three clients. Source/restore targets are normalized (including default port and protocol) and equivalent targets are rejected. Post-restore probes verify FORCE RLS under a non-superuser session, tenant-scoped outbox/checkpoint replay state, and a bounded rollback load/query soak.
- Audit/evidence behavior: `artifacts/task-0017-native-postgres-report.json` records pass/failed/skipped checks without credentials, customer data, or stderr. Missing prerequisites are `skipped`, never a false pass; command failures are `failed`.
- Tests and commands run: `node --test scripts/task-0017-native-postgres.test.mjs`; `node scripts/task-0017-native-postgres.mjs`; full repository tests; `scripts/validate.ps1`; `git diff --check`.
- Results: Native run is fail-closed until `UPFS_DATABASE_URL`, `UPFS_RESTORE_DATABASE_URL`, `psql`, `pg_dump`, and `pg_restore` are supplied. The synthetic contract tests pass; no production or customer data is used.
- Rollback/corrective-forward plan: Script and documentation are additive and revert through protected Git review. Database migrations remain corrective-forward; the runner never mutates an external target unless URLs are explicitly supplied.
- Documentation updated: This handoff.
- Known risks and follow-ups: Native RLS-scoped outbox/checkpoint/load queries require the disposable targets and credentials for the live evidence run; TASK-0018 remains blocked on managed controls.
